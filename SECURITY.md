# Security

- Keep credentials out of frontend code
- Use HTTPS
- Apply least-privilege permissions
- Validate every request
- Restrict spreadsheet access
- Log operational failures
- Avoid exposing internal error details to clients

Never commit service-account JSON, access tokens, API keys, private credentials, or production environment variables.
